// 🌐 My First Web3 Vibe Project
console.log("Welcome to Web3 world!");

// Mint a fake token
let myTokens = 0;

function mintToken() {
    myTokens += 1;
    console.log(`✨ You minted token #${myTokens}!`);
}

// Mint 3 tokens
mintToken();
mintToken();
mintToken();
